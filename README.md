# jules-sdk
An idiomatic Go client library for the Google Jules API (v1alpha). Provides a unified, strongly-typed interface for orchestrating Jules coding sessions, activities, and connected source repositories with hardened HTTP transport, cursor-based pagination, and structured error handling.
